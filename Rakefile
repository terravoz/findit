ENVIRONMENTS = {
  "test" => {
    "host" => "vlad@simone.mayfirst.org",
    "path" => "/var/local/drupal/findit/web",
    "backups" => "/tmp",
  },
}
TAGNAMES = %w(stable)
DRUPAL = "web"
PROFILE = "standard"
BUILDDIR = "/tmp/build"